worker-threads: 4
worker-fs-threads: 2
worker-grpc-threads: 2
logger-level: debug

is_testing: false

server-port: 8080
server-grpc-port: 8081

hello-endpoint: '[::1]:8081'

dbconnection: 'postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/kiddy_line_processor_db_1'
